@inherits KarbonView
@{
    Layout = "_Layout.cshtml";
    var sections = Json.Decode<IEnumerable<string>>(Model.CurrentPage.Get("Sections"));
    
}

@Html.Partial("_Welcome")

@foreach (var s in sections)
{
    @Html.Partial("_" + s)
}

